import { StandardTemplate } from "../../framework/template";

export default function Tutoring() {
    return (
        <StandardTemplate active='Tutoring' useStreaks={false}>
            <h1>Tutoring</h1>
        </StandardTemplate>
    )
}