import { StandardTemplate } from "../../framework/template";

export default function Sync() {
    return (
        <StandardTemplate active='Synchronizers' useStreaks={true}>
            <h1>Sync (nutQ)</h1>
        </StandardTemplate>
    )
}